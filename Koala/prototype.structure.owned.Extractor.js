// StructureExtractor.prototype.init = function(pProxy)
// {
//     Log(undefined,'STRUCTURE EXTRACTOR: '+this.pos.toString()+' init - '+pProxy.id+' proxy: '+pProxy);
//     pProxy.mineralSource = _.find(PCache.getEntityCache(ENTITY_TYPES.mineral), (aProxy) => aProxy.pos.isEqualTo(this.pos));
// }
